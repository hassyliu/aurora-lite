本版新增紧凑服务器列表、自动下发与运行中编辑、实时日志弹窗，以及落地选择回显。

### 下载

- **Debian 13 / AMD64 二进制包**：`aurora-lite-0.3.0-linux-amd64-debian13.tar.gz`，同时下载对应 `.sha256` 校验文件。
- 源码包：`aurora-lite-0.3.0-source.tar.gz`。
- [现有 v0.2.1 二进制版升级步骤](https://github.com/hassyliu/aurora-lite/blob/v0.3.0/UPGRADE-0.3.0.md)。保留密码、数据库、密钥和 Nginx 配置，无需重新初始化。

### 更新内容

1. 每台服务器只占一行，支持名称 / IP / 备注搜索，拖动与上下移动排序；自定义顺序永久保存，也可按名称、地址或规则数查看。
2. 新建规则保存后自动下发；运行中的规则可直接编辑，保存后清理旧配置并启动新配置。手动终止的规则编辑后保持停止，点击“启用”恢复。
3. 执行日志每秒在弹窗刷新，服务日志每 3 秒刷新；服务器和状态检测详情直接在弹窗查看。
4. 编辑时保留上次选择的落地。手动修改目标会解除选择，删除落地不会更改已有规则中的目标地址。
5. 连接检查增加 conntrack 状态；修正规则更新时的旧配置清理与失败状态显示。

### 验证与运行范围

- GitHub Actions 在 Debian 13 AMD64 环境构建，测试通过后才发布附件；包含实际 GOST TCP / UDP 回环转发测试。
- 二进制完成初始化、登录、API、静态资源、备份与 doctor 冒烟测试，主控不依赖 PATH 中的系统 Python。
- Windows 浏览器已验证 12 台服务器列表、排序、搜索、规则直接编辑、停止状态、落地回显和日志刷新。
- 实际 VPS 上的 systemd、iptables 转发及公网端口放行仍需按业务环境验证。修改运行中的规则会短暂重启该条转发；同一服务器的后台任务串行执行。

首次启动自动增加数据库字段，原有手动停止的规则不会因升级而启用。
