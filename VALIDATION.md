# 验证记录

## 0.3.0 本地回归验证（2026-09-11）

环境：Windows 11、项目独立 Python 虚拟环境、锁定依赖。运行 `python -m unittest discover -s tests -v`：**36 项中 35 项通过，1 项跳过**（未配置真实 GOST 可执行程序）。`node --check aurora/static/app.js` 和 `pip check` 通过。

新增验证覆盖服务器排序持久化和非法排序原子拒绝；新建自动下发、忙时拒绝冲突操作、运行中直接编辑和手动停止后保持停止；修改失败后的旧配置清理、实际部署配置与新配置不一致时不报告成功；落地关联回显、手动解除和删除后保留目标；旧库迁移保留顺序、密钥和数据；任务进行中读取日志、分段 SSH 输出、日志脱敏以及停止服务后计数不被错误归零。

浏览器使用 12 台模拟服务器验证单行布局、搜索、上下移动、拖动和按规则数量排序；新建规则自动执行、运行中修改监听端口、上次落地回显、停止后编辑、服务日志持续刷新、服务器详情弹窗。390px 窄屏页面无整体横向溢出，服务器列表独立横向滚动。浏览器控制台无脚本错误。

本轮未连接真实 VPS，未执行 Linux systemd / iptables / GOST 实际转发或构建 Linux 二进制。下方为历史版本的验证记录，不能代替 0.3.0 的 VPS 验收。

---

日期：2026-09-10。环境：Windows 11，Python 3.12.14，GOST v3.3.0（官方 Windows AMD64 发行包，SHA-256 已核对）。

## 已完成

完整测试集：**15 项通过**，无失败。

| 验证 | 结果 |
|---|---|
| 登录、会话保护、来源检查、登录频率限制 | 通过 |
| 修改密码后撤销已有会话 | 通过 |
| SSH 凭据加密存储，API/导出不返回凭据 | 通过 |
| 只接受 iptables/GOST，拒绝无效地址与命令片段 | 通过 |
| 端口冲突、SSH 端口保护、IP 版本校验 | 通过 |
| 规则保存→任务下发→停止→编辑→删除 | 使用受控远程替身完成 |
| 失败任务不报告成功，任务中断状态持久化 | 使用受控远程替身完成 |
| 同一计数周期累计、新周期恢复和计数归零 | 通过 |
| SQLite 完整备份、恢复与凭据解密 | 通过 |
| 数据目录防重复启动，丢失密钥时拒绝覆盖 | 通过 |
| iptables 专属链、DNAT 连接范围、IPv6 配置生成 | 静态/单元验证通过 |
| GOST 双协议与域名配置、指标解析 | 通过 |
| **GOST 实际 TCP、UDP 转发与非零流量指标** | 回环临时端口测试通过 |
| **真实 Paramiko SSH 握手、密码认证及数据传输** | 本地 SSH 测试服务通过 |
| SSH 指纹不符时在发送密码前拒绝连接 | 通过 |

Python 文件编译检查、JavaScript 语法检查、`pip check` 均通过。

浏览器验证：登录、服务器表单保存、GOST TCP+UDP 规则保存、生成配置预览、方式筛选、桌面页面布局正常。窄屏页面的 DOM 控件可见；完整移动端人工验收仍可在正式部署后补充。浏览器测试使用 `work/preview-data` 中的独立虚构数据，没有连接任何真实中转机；这些数据不包含在发行包内。

测试输出包含两个非失败提示：当前 Starlette 提示其 TestClient 将迁移到 httpx2；SSH 的负向指纹测试可能记录连接重置。两者不影响断言结果。

## Debian 13 实机验收（2026-09-10）

环境：Debian 13.4 / AMD64、Linux 6.12、系统 Python 3.13.5 + 项目 venv、systemd 257、iptables 1.8.11（nf_tables）、GOST 3.3.0 官方 Linux 发行包（安装时核对 SHA-256）。未使用 Docker、PostgreSQL、Redis。

主控使用 `aurora-panel` 专用系统账号运行，代码在 `/opt/aurora-lite-panel`，数据在其 `data` 目录。通过受限本机 SSH 密钥管理同机中转服务。

| 实机验收项目 | 结果 |
|---|---|
| 系统 Python 创建 venv、安装依赖、注册 systemd | 通过；主控 active + enabled |
| systemd 单元语法检查、服务账号读取代码与写 SQLite | 通过 |
| 浏览器访问、管理员登录、真实节点/规则/流量显示 | 通过 |
| 主控经 SSH 校验指纹、认证、检查节点与安装组件 | 通过 |
| GOST TCP 与 UDP 真实转发 | 通过；TCP 23,552 字节完整回显，UDP 12 个数据报逐个核对 |
| iptables TCP 与 UDP 真实转发 | 通过；TCP 27,648 字节完整回显，UDP 12 个数据报逐个核对 |
| 后台正常轮询采集流量 | 通过；两种方式的入站/出站均为非零 |
| GOST Unix socket 指标与 systemd 权限限制 | 通过 |
| 两种中转规则的 systemd 服务重启 | 通过；重启后 TCP、UDP 均能转发 |
| 停止主控时中转是否继续 | 通过；主控停止期间 TCP、UDP 仍能转发 |
| 主控重启、SQLite 规则/会话/累计流量保留 | 通过；累计值在中转服务换计数周期后继续增加 |
| 停止规则后 TCP、UDP 不再转发 | 通过 |
| 停止→修改监听端口→重新下发→转发 | 两种方式均通过 |
| 经面板读取远程规则日志 | 两种方式均通过 |
| 经面板移除规则、清理规则链与连接跟踪 | 通过；无测试规则、服务文件、DNAT 连接残留 |
| 完整自动化测试集在 Linux 运行 | **15 项通过**，无失败 |
| Linux `pip check` | 通过 |

测试流量从 Windows 电脑进入服务器的真实网卡，再转发至临时网络命名空间内的 TCP/UDP 回显服务；不是仅在面板上生成配置，也不是主机本地回环代替 iptables 入站测试。

流量示例：首次正常轮询后，iptables 累计入站 40,410 字节、出站 40,370 字节；经过规则服务与主控重启后累计为 50,450 / 50,250 字节。GOST 同样完成非零采集和跨重启累计。iptables 包含网络层开销，与 GOST 应用流量口径不同。

首次部署发现并修复两处问题：systemd `WorkingDirectory` 不接受原写法中的路径引号；DNAT 目标规则指定端口时需要显式声明 TCP/UDP 协议，不能依赖父链匹配。修复已同步到源码与服务器，并完成上述实测。

GOST 初始化时可能记录无法创建 `.gost` 证书目录的提示（服务根文件系统只读）；当前仅用 TCP/UDP 转发，不依赖该证书目录，转发与指标均正常。

临时测试规则、回显服务、网络命名空间和 veth 已清理，IPv4 forwarding 恢复测试前的值。保留本机节点，面板可供继续添加实际规则。验收任务日志保留，包含修复前一次失败及修复后的成功记录。

## 服务器内转发管理页面更新（2026-09-10）

页面入口改为“服务器 → 单台服务器 → 转发管理”。规则列表、搜索、方式筛选和流量合计均限定当前服务器，新增规则固定归属该服务器。服务器卡片、总览中的规则链接和详情页返回入口已一并更新。

本地浏览器使用独立预览数据验证了两台节点的规则分组、当前节点内新增、编辑、筛选重置、返回列表和直接打开详情地址。JavaScript 语法检查通过。预览数据没有下发到实际中转机。

## 橙红 / 米白亮色主题更新（2026-09-10）

重写了界面样式，统一背景、导航、按钮、卡片、表格、状态、图表、表单、弹窗和登录封面的颜色与间距，更新了站点图标及浏览器主题色。保留“服务器 → 转发管理”的流程。640px 以下使用底部导航，窄屏表单采用单列；侧栏账号区隐藏时可在“设置”中退出登录。

- 桌面浏览器检查：总览、服务器卡片、转发管理、编辑弹窗、配置预览、任务页、设置与登录页。
- 390 × 844 视口检查：登录、服务器列表、底部导航、转发管理、单列弹窗、设置、退出登录；检查页面宽度未出现横向溢出，规则表格保持独立横向滚动区域。
- 验证主文字、辅助文字、按钮文字、表单标签与正常状态文字的配色对比度，均不低于 4.5:1。
- JavaScript 语法检查通过；本次不涉及转发引擎或数据库结构修改。

## v0.2.0 落地管理与状态检测（2026-09-11）

新增 8 项自动化测试，完整测试集在 Debian 13 上 **23 项全部通过**，`pip check` 通过。本地新增测试和其他原有测试通过；一次 GOST 测试因调用时误填二进制路径失败，纠正路径后单独重跑通过。

- 落地 CRUD、域名归一化、排序持久化、旧排序列表冲突与重复 ID 拒绝；修改 / 删除落地不影响已保存规则。
- SQLite schema 1 → 2 的增量迁移，原规则与凭据可用。
- 服务器连接结果保存，失败信息隐藏凭据；编辑连接时清除旧检测结果。
- 检测任务只读，不改变期望启停状态；中断时不把运行服务误标为已停止。
- 实际回环 TCP 可达 / 端口关闭检测；同机 iptables 与 UDP 不报告完整转发成功。

浏览器验收：添加 IP 和域名落地、真实拖动手柄改变顺序并刷新确认、上移按钮、转发目标自动填入、服务器检查中 / 成功 / 失败的卡片反馈、检测详情自动更新。390 × 844 窄屏检查了五项底部导航与落地列表，无页面横向溢出。

Debian 实机通过面板任务接口完成：

| 检测场景 | 结果 |
|---|---|
| 真实 SSH 与管理权限检查 | 通过，结果保存到节点 |
| 落地添加、排序、读取、删除 | 通过 |
| GOST TCP 健康服务 | 服务、配置、监听、指标、目标和入口 TCP 均通过 |
| iptables TCP + UDP 健康规则 | 内核规则、指标、目标 TCP 通过；UDP 与同机入口明确待验证 |
| GOST UDP 健康服务 | 监听与指标通过；业务转发明确待验证 |
| 关闭目标 TCP 回显服务 | 检出目标连接失败，整体显示异常 |
| 删除测试规则的一条 PREROUTING 跳转 | 检出内核规则缺失；重新下发后恢复 |
| 停止 GOST 服务后检测 | 显示未启用 |

各测试规则另外从 Windows 进行了对应协议的真实回显检查。测试完成后，临时转发、落地地址、回显服务与网络命名空间已清理，IPv4 forwarding 恢复测试前的值。

## v0.2.1 Linux 单文件程序（2026-09-11）

在 Debian 13 AMD64、glibc 2.41、Python 3.13.5 上使用 PyInstaller 6.22.2 构建。可执行程序 23,969,640 字节，SHA-256 为 `36622728179e6e880cfcebf430fe636523ba055f64b6ab489234c31f333dd5d3`。

二进制在独立目录、独立数据库及临时 18000 端口运行，账号为 `aurora-panel`。systemd 将系统 Python 程序、标准库、系统 libpython、原项目虚拟环境、构建虚拟环境及构建源码设为不可访问，环境 `PATH=/nonexistent`。此条件下通过：

- 管理员登录，HTML / CSS / JS / 图标与源码逐个 SHA-256 比对。
- SQLite 落地 CRUD 和排序、真实 SSH 认证及状态结果持久化。
- 通过二进制面板下发 GOST TCP、GOST UDP、iptables TCP+UDP，并从 Windows 完成实际回显。
- 正常状态检测、关闭目标后的失败检测、缺失内核跳转检测、修复及停用状态。
- 程序自身的 TCP 检测子进程，分别验证实际开放端口与关闭端口。
- systemd 重启后会话、落地记录与节点检查结果保留。
- 不依赖当前工作目录的数据路径、CLI 交互初始化、完整性检查及包含密钥的 SQLite 备份。

同版本源码完整自动化测试 **23 项通过**。临时规则、地址、网络命名空间、测试服务和包含凭据的测试数据库已清理。原 v0.2.0 正式服务继续运行，未切换启动方式。

## 验证范围之外

- 尚未验证真实公网路径、云安全组及复杂既有防火墙的组合。
- IPv6 配置有单元验证，本轮没有实际 IPv6 路由环境测试。
- 已验证 systemd 服务重启并检查开机启用；没有执行整机重启。
- 尚未在 Ubuntu 或大规模节点/长时间高负载场景进行实机验收。
